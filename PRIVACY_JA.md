[English](PRIVACY.md) | 日本語

# プライバシーポリシー — Arcade Counter Timer

**最終更新日：2026-09-22**
**対象：Arcade Counter Timer 0.2.0（Tokumaru Labs・公開候補）**

サーバー、アカウント、解析、広告、クラウド同期、リモートコードはありません。
利用者のデータを外部へ送信しません。

## 端末内に保存する情報

`chrome.storage.local` に、タイマー状態、セッションカウント、日別の時間・回数、
選択テーマ、時計・音・演出設定、表示方式、自由配置の座標・サイズ・固定設定、
更新案内の未表示・表示済み情報を保存します。0.2.0へ更新する際は、更新前の
タイマー・統計・設定の控えも一度だけ別に保存します。同じブラウザプロファイル内に留まります。

`chrome.storage.session` には、自由配置した画面とタブを結び付けるための
タブ識別子とランダムなトークンを一時保存します。利用者を追跡するIDではなく、
ブラウザを再起動すると消えます。

同じ拡張IDの通常更新では、端末内のデータを引き継ぎます。0.2.0では古い日別履歴の
自動間引きを行いません。旧版で既に間引かれた履歴を、この更新で復元することはできません。

## 権限とページへのアクセス

- `storage`：上記の情報を端末内に保存します。
- `activeTab`：利用者が拡張を呼び出したタブへ一時的にアクセスします。
- `scripting`：そのタブへ自由配置用のタイマーUIを挿入します。
- `sidePanel`：ブラウザのサイドパネルへタイマーを表示します。

常時のhost_permissionsや、全サイトへ自動実行するcontent scriptはありません。
挿入するスクリプトは自分自身のタイマー枠を追加し、配置のため画面サイズを取得します。
ページ本文、フォーム入力、閲覧履歴を抽出しません。ブラウザから得るタブ情報は
表示先の特定にだけ使用し、ページのURLやタイトルを保存しません。
埋め込む画面は拡張機能自身のページです。許可していない埋め込みから
サービスワーカー経由でタイマーのデータを読み書きすることはできません。

画面用の同梱ファイルを動的な拡張URLでweb-accessibleにし、挿入したiframeから
読み込めるようにしています。これは閲覧先サイトの内容へのアクセス権限ではありません。

## データの管理

- タイマー・カウント・セッションのリセットでは、過去の統計と設定を残します。
- 確認付きの **CLEAR ALL DATA** では、タイマー、回数、統計、設定、配置設定、
  更新前データの控えを削除します。統計を消した後に案内が再表示されないよう、
  更新案内の既読情報は残します。
- アンインストールでは端末内のデータも削除されます。記録を残す場合は、
  削除・再インストールではなく、既存の拡張機能を更新してください。

年齢を問わず、氏名、メールアドレス、位置情報、ページ内容、閲覧履歴を収集・販売・共有しません。

## ポリシーの変更とお問い合わせ

変更時はこのリポジトリで更新版と最終更新日を公開します。
質問：https://github.com/tokumaru-labs/arcade-counter-timer/issues
公開Issueへ個人情報を書き込まないでください。セキュリティ報告は
[SECURITY.md](SECURITY.md) の手順に従ってください。
